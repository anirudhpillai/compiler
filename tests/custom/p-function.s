fdef reverse (inseq : seq<top>) {
    i : int := 0;
    loop
        break;
    pool
    return outseq;
} : seq<top> ;

main {};

fdef reverse (inseq : seq<top>) {
    i : int := 0;
    loop
        break;
    pool
    return outseq;
} : void ;
