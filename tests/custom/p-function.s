fdef reverse (inseq : seq<top>) {
    i : int := 0;
    loop
        break;
    pool
    return outseq;
} : seq<top> ;

fdef reverse (inseq : seq<top>) {
    i : int := 0;
    loop
        break;
    pool
    return outseq;
} : void ;
